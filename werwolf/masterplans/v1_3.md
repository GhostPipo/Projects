# Masterplan: Werwolf Refactoring & MongoDB Migration

## Phase 1 & 2: Architektur & Datenmodell (Abgeschlossen)
- [x] `.env` eingerichtet, Mongoose Schema `models/Game.js` erstellt.

## Phase 3: Datenbankanbindung & RESTful API (In Arbeit / Bugfix)
- [x] Datenbankverbindung via Mongoose in `server.js` hergestellt.
- [x] `routes/gameRoutes.js` erstellt und in `app.js` eingebunden.
- [ ] **BUGFIX (404 Error):** Frontend-Backend-Mismatch beheben. Das Frontend (`script.js`) ruft beim Erstellen der Lobby einen falschen/alten Endpunkt auf. Die Fetch-Aufrufe müssen auf die neuen REST-Endpunkte (z.B. `/api/games`) umgeleitet werden.

## Phase 4: Frontend Templating & API-Integration (Startet jetzt)
- [ ] **API-Calls anpassen:** Alle `fetch()` Aufrufe in der `js/script.js` (Lobby erstellen, beitreten, Status abrufen) auf die neuen `/api/games/...` Routen umstellen.
- [ ] Frontend-Anpassung auf native HTML5 `<template>` Tags (nachdem die API-Calls wieder sauber durchgehen).